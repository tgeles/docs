<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "Git_Guide.ent">
%BOOK_ENTITIES;
]>
<chapter id="sect-Git-Using_Git_in_the_OpenShift_Documentation_Workflow-Working_with_Local_Branches">
	<title>Working with Local Branches</title>
	<table frame="all" id="tabl-Git-Using_Git_in_the_OpenShift_Documentation_Workflow-Working_with_Local_Branches-Git_commands_for_working_with_local_branches">
		<title>Git commands for working with local branches</title>
		<tgroup align="left" cols="2" colsep="1" rowsep="1">
			<colspec colname="c1" colwidth="1*"></colspec>
			 <colspec colname="c2" colwidth="2*"></colspec>
			 <thead>
				<row>
					<entry align="center">
						Command
					</entry>
					 <entry align="center">
						Result
					</entry>
				</row>
			</thead>
			 <tbody>
				<row>
					<entry>git add <replaceable>file</replaceable></entry>
					<entry>adds the specified, untracked file to the index. Also used to update files to be committed after fixing merge conflicts.</entry>
				</row>
				<row>
					<entry>git commit -m '<replaceable>message</replaceable>' <replaceable>file</replaceable></entry>
					<entry>commits changes in the specified file or directory to the local repository</entry>
				</row>
				<row>
					<entry>git commit -m '<replaceable>message</replaceable>' -a</entry>
					<entry>adds all new files to the index and commits all changes to the local repository</entry>
				</row>
				<row>
					<entry>git stash</entry>
					<entry>stashes current changes and sets branch to last commit. Useful for switching branches or pulling remote changes without having to commit local changes first.</entry>
				</row>
				<row>
					<entry>git stash apply</entry>
					<entry>applies the changes stashed using the <command>git stash</command> command</entry>
				</row>
				<row>
					<entry>git branch</entry>
					 <entry>displays local branches</entry>
				</row>
				<row>
					<entry>git branch -a</entry>
					<entry>displays all local and remote branches</entry>
				</row>
				<row>
					<entry>git branch <replaceable>branch</replaceable></entry>
					<entry>creates a new local branch with the specified name</entry>
				</row>
				<row>
					<entry>git checkout <replaceable>branch</replaceable></entry>
					<entry>checks out the specified local branch. If you checkout another branch without committing changes in the current branch, the uncommitted files will be copied to the new branch.</entry>
				</row>
				<row>
					<entry>git branch -d <replaceable>branch</replaceable></entry>
					<entry>deletes the specified local branch if it has been merged</entry>
				</row>
				<row>
					<entry>git branch -D <replaceable>branch</replaceable></entry>
					<entry>deletes the specified local branch regardless of its merge status</entry>
				</row>
			</tbody>
		</tgroup>
	</table>
</chapter>