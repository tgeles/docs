<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "Git_Guide.ent">
%BOOK_ENTITIES;
]>
<chapter id="sect-Git-Using_Git_in_the_OpenShift_Documentation_Workflow-Working_with_Remote_Branches">
	<title>Working with Remote Branches</title>
	<table frame="all" id="tabl-Git-Using_Git_in_the_OpenShift_Documentation_Workflow-Working_with_Local_Branches-Git_commands_for_working_with_remote_branches">
		<title>Git commands for working with remote branches</title>
		<tgroup align="left" cols="2" colsep="1" rowsep="1">
			<colspec colname="c1" colwidth="1*"></colspec>
			 <colspec colname="c2" colwidth="2*"></colspec>
			 <thead>
				<row>
					<entry align="center">
						Command
					</entry>
					 <entry align="center">
						Result
					</entry>
				</row>
			</thead>
			 <tbody>
				<row>
					<entry>
						git checkout --track origin/<replaceable>branch</replaceable>
					</entry>
					 <entry>
						creates and checks out a local tracking branch for the specified remote branch
					</entry>
				</row>
				<row>
					<entry>git fetch origin</entry>
					<entry>fetches the remote repository and stores it locally, but does not merge it with the current branch</entry>
				</row>
				<row>
					<entry>git pull</entry>
					<entry>fetches and merges the remote repository with the local repository</entry>
				</row>
				<row>
					<entry>git pull origin <replaceable>branch</replaceable></entry>
					<entry>fetches and merges the specified remote branch with the current local branch</entry>
				</row>
				<row>
					<entry>git push</entry>
					<entry>pushes all committed changes on all branches to the remote repo</entry>
				</row>
				<row>
					<entry>git push origin <replaceable>branch1</replaceable>:<replaceable>branch2</replaceable></entry>
					<entry>creates a remote branch <replaceable>branch2</replaceable> and pushes the local branch <replaceable>branch1</replaceable> to it</entry>
				</row>
				<row>
					<entry>git push origin :<replaceable>branch</replaceable></entry>
					<entry>deletes the specified remote branch</entry>
				</row>
				<row>
					<entry>git reset --hard origin/<replaceable>branch</replaceable></entry>
					<entry>resets the checked-out local branch to the status of the specified remote branch; running <command>git fetch origin</command> first is recommended</entry>
				</row>
			</tbody>
		</tgroup>
	</table>
</chapter>